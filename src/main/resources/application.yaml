spring:
  application:
    name: batch-service
  batch:
    job:
      enabled: true
    jdbc:
      initialize-schema: always
  datasource: 
    url: jdbc:mysql://localhost:3306/cartdb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update